import styled from 'styled-components';

export const RedIcon = styled.span`
    cursor: pointer;
    transition-duration: 0.1s;
    margin-left: 16px;

    &:hover {
        color: rgb(255, 0, 0);
    }
`;